name: SaydiVoice D5A Read-Only Voice Style

on:
  push:
    branches:
      - d5/voice-style-discovery-20260917
  workflow_dispatch:

permissions:
  contents: read

jobs:
  d5a-readonly:
    runs-on: [self-hosted, Windows, X64]
    timeout-minutes: 12
    defaults:
      run:
        shell: powershell
        working-directory: 01_DISCOVERY/saydivoice
    env:
      PYTHONIOENCODING: utf-8
      PYTHONUTF8: '1'
    steps:
      - uses: actions/checkout@v4

      - name: Resolve installed Python
        run: |
          $python = $null
          $py = Get-Command py.exe -ErrorAction SilentlyContinue
          if ($py) {
            $candidate = (& $py.Source -3 -c "import sys; print(sys.executable)").Trim()
            if ($LASTEXITCODE -eq 0 -and (Test-Path $candidate)) { $python = $candidate }
          }
          if (-not $python) {
            $cmd = Get-Command python.exe -ErrorAction SilentlyContinue
            if ($cmd) { $python = $cmd.Source }
          }
          if (-not $python -or -not (Test-Path $python)) { throw "Installed Python 3 was not found." }
          "SAYDI_SYSTEM_PYTHON=$python" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Create isolated D5A environment
        run: |
          $venv = Join-Path $env:RUNNER_TEMP "saydi-d5a-venv"
          if (Test-Path $venv) { Remove-Item $venv -Recurse -Force }
          & "$env:SAYDI_SYSTEM_PYTHON" -m venv $venv
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          $venvPython = Join-Path $venv "Scripts\python.exe"
          & $venvPython -m pip install --upgrade pip
          & $venvPython -m pip install -r requirements-dev.txt
          & $venvPython -m pip install -e .
          "SAYDI_PYTHON=$venvPython" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Resolve installed Google Chrome
        run: |
          $chrome = Join-Path $env:ProgramFiles "Google\Chrome\Application\chrome.exe"
          if (-not (Test-Path $chrome)) { $chrome = Join-Path ${env:ProgramFiles(x86)} "Google\Chrome\Application\chrome.exe" }
          if (-not (Test-Path $chrome)) { throw "Installed Google Chrome was not found." }
          "SAYDI_CHROME=$chrome" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Verify authenticated profile exists and is unlocked
        run: |
          $profile = Join-Path $env:LOCALAPPDATA "MAGASIN\MediaRobot\saydivoice\browser_profile"
          if (-not (Test-Path $profile)) { throw "Saydi browser profile is missing." }
          $holders = @(Get-CimInstance Win32_Process -Filter "Name='chrome.exe'" | Where-Object { $_.CommandLine -like "*MAGASIN\MediaRobot\saydivoice\browser_profile*" })
          if ($holders.Count -gt 0) { throw "Saydi robot profile is currently open in Chrome." }

      - name: Run D5A read-only discovery
        run: |
          & "$env:SAYDI_PYTHON" -m saydivoice_discovery.d5_voice_style --chromium-executable "$env:SAYDI_CHROME"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Stage D5A evidence
        if: always()
        run: |
          $root = Join-Path $env:LOCALAPPDATA "MAGASIN\MediaRobot\saydivoice\runs\d5a_latest"
          $out = Join-Path $env:RUNNER_TEMP "saydi-d5a-evidence"
          if (Test-Path $out) { Remove-Item $out -Recurse -Force }
          New-Item -ItemType Directory -Path $out | Out-Null
          if (Test-Path $root) {
            Get-ChildItem $root -File | Where-Object { $_.Extension -in @('.json','.png') } | ForEach-Object { Copy-Item $_.FullName $out -Force }
          }

      - name: Upload D5A evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: saydi-d5a-${{ github.run_id }}
          path: ${{ runner.temp }}\saydi-d5a-evidence
          retention-days: 7
