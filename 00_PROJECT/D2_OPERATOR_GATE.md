# D2 operator gate

Operator action is required only after V0.3 package delivery. Run the package once on the target Windows laptop without changing SaydiVoice controls manually. Return the eight artifacts listed in `NEXT_STEP.md`. D2 must not be merged until those artifacts are reviewed.
